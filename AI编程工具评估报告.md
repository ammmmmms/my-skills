# AI 辅助编程工具使用评估报告

---

## 一、评估对象

- **基础环境**：OpenCode（终端 AI 编程工具）
- **底层模型**：DeepSeek V4 Flash
- **对比方案**：原生能力 / GSD（工作流约束型插件）/ OMO（多 Agent 编排型插件）

---

## 二、OpenCode 原生能力

### 2.1 两种对话模式

OpenCode 提供两种核心模式，Tab 键一键切换：

**Plan 模式（只读）**

只分析不改动，适合接手陌生代码、讨论技术方案。因为主要工作是搜索和分析代码，Plan 模式下反而更频繁地自动拉起子 Agent 并行搜索。

**Build 模式（可写）**

直接改文件、执行命令、写代码。日常开发主要在这个模式下进行。

### 2.2 关键指令

几个日常高频使用的内置命令：


| 命令       | 作用             | 说明                                                  |
| ---------- | ---------------- | ----------------------------------------------------- |
| `/undo`    | 撤销 AI 的改动   | 每次 AI 改动自动产生隐藏 git commit，一键回滚到改动前 |
| `/compact` | 压缩对话记录     | 聊久了早期内容精简为摘要，释放上下文空间              |
| `/init`    | 生成项目规则文件 | 扫描项目后生成规则，之后每次对话自动加载              |
| `/editor`  | 打开外部编辑器   | 复杂需求在外置编辑器里写好再提交                      |

其中 `/undo` 是最实用的——改错了能退回去，改多了能撤回来，而且可以连续 undo 多层。`/compact` 在长任务中需要手动触发，聊到 60-80K token 左右执行一次，避免 AI 因上下文过载而质量下降。

### 2.3 自定义能力

可以编写 Skill 模板（如"每次改完代码先跑 lint 再 commit"）保存复用，也可以创建自定义 Agent（如只读的代码审查员）。核心在于把重复性的行为规范固化为可复用的配置。

### 2.4 原生评价

**优点**：灵活轻量、学习成本低、不挑底层模型

**不足**：

- 子 Agent 调度随机——同一个任务有时调有时不调，没有规律
- 无流程约束——全靠使用习惯，容易遗漏验证步骤
- 上下文管理需手动——`/compact` 虽好用，但要自己记得触发

### 实操截图

> *（此处放置 OpenCode 原生界面截图：Plan/Build 模式切换、`/undo` 操作前后对比、`/compact` 执行效果）*

---

## 三、GSD — 用流程约束对抗混乱

### 3.1 核心理念

把大任务切成小阶段，每阶段产出一份精简的档案文件（markdown）。下一阶段只读上一阶段的档案，不读原始聊天记录。借此规避 AI 因对话太长而"变傻"的问题。

### 3.2 完整链路

```
map-codebase    → 自动并行侦察，产出 7 份代码库分析文档
new-project     → 定义需求，产出项目定义和路线图
discuss-phase N → 讨论某个阶段的实现细节
plan-phase N    → 拆解为原子任务清单
execute-phase N → 按 wave 机制执行（无依赖任务并行，有依赖的串行）
verify-work N   → 逐项验收
```

完成一个 Phase 后，建议新开对话窗口继续下一个 Phase，只加载档案文件作为上下文。

### 3.3 子 Agent 调度

GSD 在特定命令中内置了并行调度逻辑，不依赖模型自行判断：

- `/gsd-map-codebase`：强制启动 4 个并行 Agent 分别分析技术栈、架构、代码结构和代码约定
- `/gsd-execute-phase`：按 PLAN.md 中的依赖关系自动编排 wave，wave 内并行、wave 间串行

这种调度的可靠性高于原生——因为调度规则写在 prompt 模板里，不是模型自己决定的。

### 3.4 实际体验

**优点**：

- 流程约束强，每步有明确输入输出，不容易跑偏
- 不挑模型，DeepSeek 也能完整跑通
- 档案文件作为外部记忆体，跨会话延续上下文

**不足**：

- 文档产出偏重——完整跑一圈产出十几份 md 文件，增量小需求显得过度
- 不会自动清空上下文，需要自己记得每 Phase 间新开窗口
- 单 Phase 内（尤其 map-codebase 和 execute）上下文仍会膨胀

### 3.5 定位判断

适合 0-1 新项目开发和团队需要统一开发规范的情况。日常小改动不建议，流程成本高于开发本身。

### 实操截图

> *（此处放置 GSD 操作截图：map-codebase 并行子 Agent 输出、new-project 生成的 ROADMAP.md、execute-phase 的 wave 执行过程、最终档案文件目录结构）*

---

## 四、OMO — 用多角色协作追求效率

### 4.1 核心理念

不只有一个 AI，而是配了 11 个不同角色的 Agent：侦察兵（Explore）、文档员（Librarian）、工程师（Junior/Hephaestus）、审查员（Momus）、规划师（Prometheus）等。核心调度者 Sisyphus 会按任务类型自动决定调用哪些角色。

### 4.2 核心角色


| 角色                | 定位          | 说明                                     |
| ------------------- | ------------- | ---------------------------------------- |
| **Sisyphus**        | 主控/项目经理 | 默认对话角色，理解需求、拆任务、调兵     |
| **Prometheus**      | 规划师        | 只读，通过反复提问帮你理清方案           |
| **Atlas**           | 执行者        | 按计划逐条执行，支持断点续传             |
| Explore / Librarian | 侦察/文档     | 自动被 Sisyphus 调用，并行搜索代码和文档 |
| Junior / Hephaestus | 执行主力      | 写代码、改文件，按任务复杂度分配         |

### 4.3 三种交互方式


| 方式                     | 触发       | 行为                           |
| ------------------------ | ---------- | ------------------------------ |
| 直接对话                 | 默认       | 像同事一样讨论，确认后再动手   |
| `ulw`                    | 加在需求前 | 不问任何问题，直接干，一轮跑完 |
| `@plan` → `/start-work` | 手动词令   | 先规划后执行，中断可续传       |

日常 80% 的场景用 `ulw` 就够了——一句话扔过去，它会自动拆任务、调兵、执行、验证。

### 4.4 子 Agent 调度

侦察阶段表现稳定——Sisyphus 会自动拉起 Explore 和 Librarian 并行搜索项目和文档。

执行阶段的效果与底层模型强相关：

- Claude / GPT：会自动调用 Junior 和 Hephaestus 并行写代码
- DeepSeek：侦查阶段 OK，但写代码阶段倾向于自己干，不会主动委派

这意味着用 DeepSeek 时，OMO 的多 Agent 价值在实战中打了一定折扣。

### 4.5 配置复杂度

安装后默认配置指向 OpenAI 的模型，无法直接使用。需要手动逐一指定每个角色的模型来源，未配置的角色会回退到硬编码默认值导致报错。对模型来源单一的用户而言，这一环节耗时且容易出错。

### 4.6 实际体验

**优点**：

- `ulw` 一个词覆盖大部分日常开发场景
- 侦察阶段并行搜索有实际提速
- 支持断点续传（`@plan` → `/start-work` 链路）

**不足**：

- 配置复杂，开箱不可用
- 子 Agent 的执行阶段调度依赖模型能力，DeepSeek 下不完整
- 上下文仍会膨胀，缺乏跨 Phase 的自动隔离

### 4.7 定位判断

适合有多家模型订阅（Claude + GPT）、愿意投入时间调优的开发者。单一模型用户获得的收益有限。

### 实操截图

> *（此处放置 OMO 操作截图：Sisyphus 主动调用 explore 子 Agent、ulw 执行过程及 Wave 分配、@plan 与 Prometheus 的访谈过程、oh-my-openagent.json 配置文件、oh-my-openagent.json 配置报错与修复前后对比）*

---

## 五、对比总览


|               | OpenCode 原生 | GSD                   | OMO                        |
| ------------- | ------------- | --------------------- | -------------------------- |
| 入门难度      | 低            | 中                    | 高                         |
| 模型兼容性    | 任意          | 任意                  | 特定模型才完整             |
| 子 Agent 调度 | 随机          | prompt 强制           | runtime 级，但模型决定效果 |
| 上下文管理    | 手动 /compact | 靠流程 + 手动新开窗口 | 手动 /compact              |
| 流程约束      | 无            | 强（档案流转）        | 无                         |
| 附带文档产出  | 无            | 多                    | 少                         |
| 日常小改      | 最轻量        | 流程过重              | 合适                       |
| 0-1 新项目    | 靠个人自觉    | 最适合                | 看配置                     |
| 团队标准化    | 靠约定        | 最适合                | 看配置                     |

---

## 六、几个发现

### 1. 好工具与好模型之间存在悖论

编排型工具（OMO）的价值随模型能力上升而增加——模型越强，越能理解"什么时候该调谁"。但模型足够强时，对工具本身的依赖反而变小。

工具真正不可替代的价值不在"让模型变聪明"，而在两件事：一是管理对话长度防止模型质量下降，二是跨会话记住上次做到哪了——这两件事模型自身解决不了。

### 2. GSD 和 OMO 解决不同问题

GSD 解决"怎么做才不会跑偏"（流程约束），OMO 解决"怎么做才能更快"（并行协作）。两者不是竞品，但不宜叠加使用——复杂度远超收益。

### 3. 子 Agent 的收益集中在搜索阶段

并行搜索代码库和文档效果明确，但执行阶段的效果依赖模型对编排指令的理解。搜索密集型任务（代码分析、文档检索）是子 Agent 目前最确定的收益点。

---

## 七、建议

- **个人日常开发**：OpenCode 原生 + 自定义 1-2 个 Skill 模板，轻量实用
- **0-1 新项目 / 团队追求统一流程**：GSD 的 Phase 机制和档案流转最有价值
- **有多家模型订阅且愿意投入调试**：OMO 的多 Agent 协作值得尝试
- **不推荐**：GSD 和 OMO 同时安装叠加使用
